import { RemoveSymbolsPipe } from './remove-symbols.pipe';

describe('RemoveSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
